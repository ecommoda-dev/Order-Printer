<div dir="rtl" style="text-align: right;">

# طابعة الفواتير (`Order-Printer`)

![version](https://img.shields.io/badge/worker-v2.5.0-blue) ![version](https://img.shields.io/badge/ui-v3.5.1-lightgrey)

**بتعمل إيه:** الموظف بيسجّل دخول بـ PIN، بيختار أوردرات جاهزة للطباعة (S1 عادي · S2 استبدال/استرجاع)، بيطبع فواتيرها، والأداة بتسجّل الطباعة باسمه وبتحدّث حالة الأوردر لـ `Ready`.
**مين بيستخدمها:** مخزن
**الإصدار:** Worker `v2.5.0` · الواجهة `v3.5.1` (`TOOL_VERSION`) · `MIN_WORKER_VERSION = 2.3.0`   ← **مافيش ترفيع**، السبب تحت

> 🔴 **الواجهة المستقلة `index.html` مجمّدة على قاهرة+جيزة** (قرار أحمد
> 07-09-2026). قناة بوسطة اتضافت في **الهب بس** (`Warehouse-Operations-Center/print.html`).
> السبب: بوابة المراجعة مكرّرة حرفيًا في الملفين، وإضافة قناة تالتة كانت
> هتضاعف السطح ده وتضاعف خطر R1. الأداة دي بتفضل **نقطة رجوع** لقاهرة+جيزة.
>
> ⚠️ وعشان كده **`MIN_WORKER_VERSION` هنا بيفضل `2.3.0`** مع إن الـ Worker
> بقى `2.5.0`: الواجهة دي **مش** معتمدة على `/lookup` ولا على `type` الصريح
> ولا على أي حاجة في بوسطة، وترفيعها كان هيولّع تحذير كاذب على أي rollback
> مشروع (Standards #29 — نفس الفخ اللي اتفادى في v3.4.0).
> **الهب** هو اللي بيرفع `printer.min` — لـ `2.5.0` دلوقتي.

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/Order-Printer/
الـ Worker : https://order-printer-worker.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: order-printer-worker     ← مطابق لـ name في wrangler.toml
```

## الـ Endpoints

> الأداة بترّوت بطريقتين مع بعض: **path** لنداءات الأداة نفسها (شكل تاريخي متساب
> عمدًا)، و**`?action=`** للمشترك القياسي (Auth · Logs · Diag).

| المسار | بيعمل إيه |
|---|---|
| `POST /orders` | أوردرات S1 + S2 الجاهزة للطباعة (+ `zoneExcluded` + `printingTimeS1`/`packingTimeS1` — **S1 بس**) |
| `POST /lookup` | **(v2.4.0)** أوردر **واحد** بعينه (`orderId` أو `name`) **مهما كانت حالته** — بنفس شكل صف `/orders`. قراءة بحتة |
| `POST /invoice` | بيانات فاتورة **أحدث دورة** إرجاع/استبدال (+ `truncatedLineItems` · `truncatedReturns` · `multiCycle` · `cycleCount`). **من v2.4.0** بياخد `type` اختياري وصريح |
| `POST /track` | تسجيل الطباعة + تاج + ميتافيلد الوقت + الحالة `Ready`. **بيطلب `employee`** · بياخد `guard` اختياري → `extra.guard` |
| `POST /logs` | سجل الطباعة بعدّاد لكل أوردر (+ `cap` · `total` · `truncated`) |
| `POST ?action=bosta_lookup` | أرقام أوردرات → شحنات بوسطة. **قراءة بحتة على بوسطة**؛ الكتابة الوحيدة صف `not_found` في D1 |
| `POST ?action=bosta_awb` | البوليصة كـ base64 PDF — مفردة (v1) أو مجمّعة (`mass-awb` · v2) + حارس عدّ الصفحات |
| `GET ?action=check_employee` · `POST register_pin` · `POST verify_employee` | Universal D1 Auth |
| `GET ?action=log_logout` · `GET ?action=get_employees` | Universal D1 Auth |
| `GET ?action=get_logs` · `get_logs_count` · `get_logs_export` | سجل العمليات القياسي |
| `GET ?action=diag` | فحص ذاتي بدون كتابة — متغيرات · D1 · موظفين · OAuth · صلاحيات · Origin |
| `GET ?action=get_config` | `WORKER_VERSION` — الواجهة بتقارنه بـ `MIN_WORKER_VERSION` |

## D1

```
tool  : order_printer      · type : S1 · S2 · AWB · not_found · login · logout
tool  : metafields_change  · type : update      ← الأداة بقت كاتب تاني تحت الصف ده
```

> 🔴 **`AWB` و`not_found` جداد في v2.5.0 ولسه ما اتسجّلوش في
> `ecommoda-constants` §7** — بند مفتوح تحت، ولازم يتقفل.
>
> - **`AWB`** = اتطبعت بوليصة بوسطة. الصف ده **ماشي في نفس مسار `S1`** على
>   شوبيفاي (تاج `Printed(S1)` · `printing_time_s1` · الحالة `Ready`) —
>   الفرق في **المستند المطبوع** بس.
> - **`not_found`** = أوردر `Other_Regions` **مالوش شحنة على بوسطة**. ده
>   الأثر الوحيد الموجود لتخطّي **بوابة رفع إلزامية**
>   (`ecommoda-order-lifecycle` → `zone-routing.md` §2.2)، وهو الرقم اللي
>   بيقفل نص السؤال المفتوح في G-15.
>
> ⚠️ **`bosta_awb_print`** — قيمة `tool` من أداة تجريبية سابقة، فيها صفوف
> `print` · `not_found` · `login` · `logout` من 05-09-2026. **الصفوف بتتساب
> زي ما هي بقرار** (أحمد 07-09-2026)، بس القيمة **لازم تفضل مسجّلة** في
> §7 كقيمة **متقاعدة** وإلا بتبقى مجهولة في `logs` للأبد. الأداة دي
> اتقفلت والـ Worker بتاعها (`bosta-awb-print-worker`) اندمج هنا.

✅ **مسجّلة بالكامل في `ecommoda-constants` §7** — اتسجّلت في المهارة **v1.4.4**
(04-09-2026) واتأكّدنا منها على **v1.5.2** يوم 05-09-2026: صف الأداة بقى
`order_printer` = `S1 · S2 · login · logout`، والكتابة تحت `metafields_change` /
`update` بمفتاح إسناد `extra.source = "order_printer"` مسجّلة في **جدول كُتّاب
`metafields_change`** تحت §7. مفيش شغل مطلوب على المهارة للبند ده.

⚠️ **عمود `order_id`:** الصفوف الجديدة بترجّع **الرقم المجرد** (`5678901234567`)
تنفيذًا لقاعدة الـ numeric order ID. الصفوف التاريخية (قبل v2.0.0) فيها الـ GID
كامل (`gid://shopify/Order/...`) — أي استعلام بيقارن العمود ده لازم ياخد باله.

## تسجيل الدخول

Universal D1 Auth كامل — شاشة دخول بـ PIN على خطوتين، و`currentEmployee` في
ذاكرة الـ JS بس (عمره ما يروح `localStorage`). الـ `WORKER SECRET` هو الحقل
الوحيد في شاشة الإعدادات؛ الـ `WORKER_URL` بقى **ثابت في `§CONFIG`** مش حقل
إدخال (Standards #28).

> قبل v2.0.0 مكانش فيه شاشة دخول خالص، فعمود `employee` **فاضي في كل الصفوف
> التاريخية** — مش هيتملي بأثر رجعي.

## ثوابت مزروعة في الكود (مش في `[vars]`)

```
DATE_FROM   = "2026-04-01"                    ← الأقدم من كده مبيظهرش خالص
ZONE_FILTER = ["Cairo+Giza", "Show_Room"]     ← بيفلتر، بس بقى بيرجّع zoneExcluded
LOGS_FETCH_MAX  = 5000                        ← سقف /logs — بيرجع كـ cap
LOG_EXPORT_MAX  = 2000                        ← سقف get_logs_export
```

## المضبوط فعليًا في الداشبورد

```
Bindings : DB → ecommoda-dev-logs
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET · BOSTA_API_KEY   ← الأخير جديد في v2.5.0
Vars     : SHOP_DOMAIN                        ← من [vars] في wrangler.toml
Build watch paths : * (الافتراضي — التضييق ما اتعملش)
compatibility_date: 2026-09-03                ← اتحرّك 03-09-2026 بعد فحص الأعلام (R12)
```
> 🔗 **`WORKER_SECRET` مشترك — مجموعة `warehouse_ops`.** القيمة **واحدة**
> في `order-printer-worker` و`orders-packing-checker-worker` و
> `order-item-remover-worker` (`ecommoda-constants` §6 · مجموعات السر).
> **متغيّرهاش في أداة واحدة** — التغيير بيكسر الباقي. التدوير بيمسّ التلاتة
> مع بعض + Promote للتلاتة + حقل السر في الهب.
> `employees-admin-panel-worker` **مستثنى صراحةً** — أداة إدارية وسرها فريد للأبد.
> البصمة القصيرة في `?action=diag` بتثبت إن التلاتة على نفس القيمة (الطول لوحده مابيثبتش).

تصنيف الـ `env.*` (`ecommoda-tool-migration-playbook` §4-أ-٢):

| المتغيّر | التصنيف |
|---|---|
| `WORKER_SECRET` · `CLIENT_ID` · `CLIENT_SECRET` · `BOSTA_API_KEY` | سر — يدوي دايمًا |
| `SHOP_DOMAIN` | var بيفشل لو غاب — و`assertEnv` بقى بيسمّيه بالاسم |
| — | var ليه fallback: **مفيش ولا واحد** ✅ |

## CORS

**Option B — allowlist**: `['https://ecommoda-dev.github.io']` + `Vary: Origin`.
اتغيّرت في v2.0.0 من `wildcard *` — الأداة كتابة على شوبيفاي. البوابة الفعلية
لسه `WORKER_SECRET` في كل نداء.

## خط الأساس قبل النقل

```
tool = 'order_printer'  —  قراءة 03-09-2026
S1 : 4,183 صف  (آخر صف 2026-09-02T14:00:38Z)
S2 :   186 صف  (آخر صف 2026-09-02T12:30:28Z)
```

```sql
SELECT type, COUNT(*) AS n, MAX(timestamp) AS last_ts FROM logs WHERE tool = 'order_printer' GROUP BY type;
```

## `compatibility_date` — اتحرّكت 03-09-2026 (R12)

```
2025-01-01  →  2026-09-03
```

**التاريخ ده بيعمل إيه:** بيقول لكلاودفلير «الكود ده اتكتب على السلوك اللي كان
موجود في اليوم ده». أي إصلاح **بيغيّر سلوك** ونزل بعد التاريخ ده مابيوصلش
للأداة إلا لما التاريخ يتحرّك بإيدك — عشان إصلاح في الـ runtime ما يكسرش أداة
شغّالة من غير ما حد يعمل deploy.

⚠️ **مش نسخة الـ runtime.** الـ Worker شغّال على أحدث runtime دايمًا مهما كان
التاريخ، وباتشات الأمان بتوصل عادي. المتجمّد هو **السلوك** بس.

**الفحص اللي اتعمل قبل التحريك** — كل سلوك اتغيّر افتراضيًا في الفترة دي
اتقارن بالكود الفعلي للأداة دي، مش بالتخمين ومش نقلًا عن أداة تانية:

| العَلَم | من | الأثر المحتمل | على الأداة دي |
|---|---|---|---|
| `fetch_iterable_type_support` | 2026-02-19 | **كاسر** — `Array` كـ body مابقاش يتحوّل لنص | ✅ آمن — كل `body:` في الملف `JSON.stringify(...)` (نص) |
| `strip_authorization_on_cross_origin_redirect` | 2025-09-01 | ترويسة `Authorization` بتتشال عند redirect لأصل تاني | ✅ آمن — مفيش `Authorization` في أي نداء **صادر**؛ شوبيفاي بتاخد `X-Shopify-Access-Token`. الترويسة دي بتتقرا من الطلب **الوارد** بس |
| `enable_nodejs_global_timers` | 2026-02-10 | `setTimeout` بترجّع `Timeout` مش رقم | ✅ آمن — كل النداءات جوّه `new Promise(r => setTimeout(r, …))` ومفيش استخدام لقيمة الإرجاع |
| `nodejs_compat` + `_v2` | 2026-08-04 | بيتفعّلوا افتراضيًا ويضيفوا globals (`process` · `Buffer`…) | ✅ آمن — صفر تعارض أسماء في الملف |
| `urlpattern_standard` · `strip_bom_in_read_all_text` · `text_decoder_replace_surrogates` · `require_returns_default_export` · `rpc_params_dup_stubs` | 2025-05 → 2026-02 | — | ✅ غير منطبقة — `URLPattern` · `readAllText` · `TextDecoder` · `require()` · RPC كلها **صفر استخدام** |
| باقي أعلام `node:*` | 2025-09 → 2026-03 | وحدات Node إضافية | ✅ غير منطبقة — الأداة مش بتستخدم Node |

**مفيش `compatibility_flags`** — الافتراضي مظبوط، ومفيش داعي لأي استثناء.

⚠️ **لو ظهر أي سلوك غريب بعد النشر**، الرجوع خطوة واحدة: رجّع التاريخ لـ
`2025-01-01` وانشر. الأعلام كلها ليها نسخة `disable_*` كمان لو الحاجة لواحد بعينه.


## دورات الإرجاع/الاستبدال في الفاتورة — أحدث دورة بس (R7، من v2.1.0)

`handleInvoice` كان بيلف على `order.returns.nodes` **كلها** ويجمّع بنودها في
`returnItems`. أوردر عنده دورة استبدال مقفولة من شهر + مرتجع جديد مفتوح كان بيدي:

1. بنود الدورتين مع بعض → الفاتورة بتطبع بنود دورة **اتسوّت خلاص** كأنها راجعة دلوقتي
2. `returnedIds` ملوّث ببنود قديمة → بيستبعد بند صالح من `exchangeItems`

**النتيجة: ورق غلط بيوصل للعميل.** (`ecommoda-order-lifecycle` Rule 15 ②)

**التحقق على أوردرات حيّة (03-09-2026):**

| الأوردر | الدورات | قديم | جديد |
|---|---|---|---|
| `#30134` | ٤ | ٤ بنود مرتجعة من ٤ دورات، تلاتة اتسوّوا من شهور | بند واحد (أحدث دورة) |
| `#31718` | ٢ | بندين | بند واحد |
| `#31387` · `#52402` | ١ | — | **بدون أي فرق** ✅ |

- الدورات بتتفلتر من `CANCELED`/`DECLINED` وبتترتّب بالـ **`createdAt`** —
  مش بترتيب المصفوفة، شوبيفاي مش ضامناه. والمقارنة `new Date()` مش نصيًا.
- الرد بقى فيه **`multiCycle`** و**`cycleCount`**، والواجهة بتنبّه المغلِّف
  «الأوردر ده عنده N دورات — الفاتورة بتعرض أحدث واحدة».

> ⚠️ **Pack Checker محمي من نفس المشكلة بالصدفة** — شرط `if (qty > 0)` جوّه
> `classifyOrderItems` بيرمي بنود الدورات المنتهية. الطابعة مافيهاش الشرط ده
> لأنها بتقرا الكمية من `returnLineItems.quantity` مباشرةً. متفترضش إن الحماية
> موجودة هنا لمجرد إنها موجودة هناك.

## فشل دفعة الطباعة الكبيرة — R15 (من v3.4.0 / Worker v2.2.0)

**البلاغ:** 05-09-2026 الساعة 07:02 بتوقيت القاهرة، لابتوب المخزن. الموظف
(`Abo_Selim`) حدّد **٥٠ أوردر** وضغط «طباعة المحدد»، فطلعت رسالة حمرا واحدة:
«خطأ: تعذّر الوصول للـ Worker — تأكد من الاتصال بالإنترنت أو من إعدادات الـ CORS».
قفلوا وفتحوا أكتر من مرة، ونفس النتيجة. النت كان شغّال.

**الرسالة دي مش CORS ومش النت.** البادئة `خطأ: ` مصدرها `catch` واحد بس في
`executePrint`، والنص اللي بعدها بيتولّد من مكان واحد: لما `fetch()` نفسها
**ترفض على مستوى الشبكة**. رد خطأ من الـ Worker (401/500) بيدي رسالة تانية
خالص — رسالة الـ Worker نفسها.

**التشخيص من D1 (نفس اليوم):**

| الوقت (UTC) | الصف |
|---|---|
| 04:00:01 | `login` · Abo_Selim |
| 04:02:01 | `S2` · #52785 — نجح |
| 04:02:01 | `S2` · #52645 — نجح |
| 04:10:36 | `login` · Abo_Selim — قفل وفتح تاني |
| — | **ولا صف `S1` واحد** |

صفر صفوف `/track`. ولو الـ ٥٠ نداء `/track` هم اللي فشلوا كانوا هيتمسكوا
فرديًا وتظهر **نافذة النتايج** فيها ٥٠ أحمر، مش توست — فالموت حصل قبلها، في
مرحلة `/invoice`.

**والدليل القاطع إن المشكلة مش اللابتوب ولا النت:** الساعة 04:14 نفس اليوم،
`Ahmed_Ibraheem` طبع **أوردرين** — أولهم `#53528`، وهو **أول أوردر في نفس
الدفعة الفاشلة** — على نفس الـ Worker وبنفس السيكرت، ونجحوا. المتغيّر الوحيد
كان **٢ مقابل ٥٠**.

**السبب:** `executePrint` كانت بتعمل `Promise.all` على المصفوفة كلها بلا أي حد
تزامن. ٥٠ أوردر = ٥٠ `fetch` في نفس اللحظة، وكل واحد بيجرّ OPTIONS preflight
معاه (`Authorization` + `Content-Type: application/json` بيلزّموا الـ preflight،
وماكانش فيه `Access-Control-Max-Age` فكاش كروم ٥ ثواني بس) = **١٠٠ طلب متزامن**.
وكل `/invoice` في الـ Worker كان بياخد توكن OAuth **جديد** — `getAccessToken`
ماكانش متكاش خالص — يعني ١٠٠ نداء OAuth زيادة على شوبيفاي في الدفعة الواحدة.
و`Promise.all` معناها: **واحد يفشل → الخمسين يتلغوا**، بلا طباعة جزئية وبلا
إشارة لأنهي أوردر وقع.

**وليه دلوقتي؟** ميرج `f9e67e4` نزل **04-09-2026 الساعة 06:34** وفيه R7+R8،
اللي خلّوا `INVOICE_QUERY` أتقل بمراتب (`returns` من `first: 3` لـ `first: 10`،
و`returnLineItems`/`exchangeLineItems` `first: 50` بمتداخلات). يوم 04-09 **مفيش
ولا صف طباعة واحد في D1**، فدفعة 05-09 الصبح كانت **أول دفعة مخزن حقيقية بعد
النشر**. الدفعات الكبيرة اللي نجحت قبل كده (٥٥ · ٥٢ · ٤٨ يوم 28→31 أغسطس)
كلها كانت على الاستعلام الخفيف.

### اللي اتعمل

| # | التغيير | فين |
|---|---|---|
| ① | `mapLimit` — حد تزامن `PRINT_CONCURRENCY = 4` على `/invoice` و`/track` | `index.html` §TOOL-JS |
| ② | فشل أوردر مايلغيش الدفعة — بيتطبع اللي نجح، والفاشل بيظهر بالاسم في نافذة النتايج | `index.html` `executePrint` |
| ③ | كاش توكن OAuth في ذاكرة الـ isolate + وعد مشترك يمنع تكرار النداء | `index.js` §SHOPIFY |
| ④ | `Access-Control-Max-Age` في `getCORS` | `index.js` §CORS |

**دفعة ٥٠ أوردر — قبل وبعد:**

| | قبل | بعد |
|---|---|---|
| طلبات خارجة من المتصفح مرة واحدة | ١٠٠ | ٤ |
| نداءات OAuth على شوبيفاي | ١٠٠ | نداء أو تلاتة |
| أوردر واحد فشل | الدفعة كلها تتلغي | يتطبع ٤٩ والفاشل بيتحدد بالاسم |

⚠️ **`MIN_WORKER_VERSION` فضلت `2.1.0`** مع إن الـ Worker بقى `2.2.0`. التلاتة
والأربعة **شفافين تمامًا للواجهة** — الواجهة مش معتمدة على أي حاجة جديدة في
الـ Worker، فترفيع الحد الأدنى هنا كان هيولّع تحذير كاذب عند أي rollback
مشروع، وده بالظبط الباج اللي Standards #29 اتكتب عشانه.

⚠️ **كاش التوكن في ذاكرة الـ isolate — مش KV ومش Cache API.** الاختيار مقصود:
مفيش binding جديد، والتوكن عمره ما بيتكتب على أي تخزين دائم. الثمن إن كلاودفلير
ممكن تشغّل أكتر من isolate تحت الحمل، فالنتيجة «نداء أو تلاتة» مش «نداء واحد
مضمون» — وده كافي للغرض. ومعاه `invalidateAccessToken()` بتتنادى من `shopifyGQL`
على **401**، وإلا توكن ملغي كان هيفضل في الكاش لحد ما الـ TTL يخلص.

## حارس الفاتورة اللاغية — v3.5.0 / Worker v2.3.0 (قرار أحمد 06-09-2026)

**المشكلة:** أوردر بيتطبع → الحالة بتبقى `Ready` → العميل بيعدّل → الحالة بترجع
`Confirmed + Edit` → الأوردر بيظهر في الطابور تاني. **الورقة المطبوعة الأولى
لسه موجودة في المخزن بمحتوى قديم**، ومحدش بيقول للموظف يقطّعها.

لإن الطباعة **بتحوّل** الحالة لـ `Ready`، رجوع الأوردر لـ `Confirmed + Edit`
معناه إن التعديل حصل **بعد** الطباعة. القاعدة مقفولة، مش احتمال.

| S1 | أثر طباعة | المعنى | الحارس |
|---|---|---|---|
| `Confirmed + Edit` | مفيش | اتعدّل قبل ما يتطبع أصلاً — عادي | مفيش |
| `Confirmed + Edit` | فيه | 🔴 فاتورة لاغية في المخزن | **تأكيد إلزامي لكل أوردر** |
| `Confirmed` | فيه | حد رجّع الحالة يدوي من `Ready` — الورقة لاغية برضه | نفس الحارس |
| `Confirmed + Edit` + `s1_packing_date_time` | فيه | 📦 الفاتورة **جوّه طرد متغلّف** | نفس الحارس + شارة «متغلّف» |

### المصدران — مش نسخة من بعض

| المصدر | بيقول إيه | حدوده |
|---|---|---|
| `custom.printing_time_s1` (ميتافيلد) | **آخر** طباعة | بيتدهس في كل طباعة (`known-gaps` G-4) — وده بالظبط المطلوب هنا |
| `printLastS1Map` (سجل D1) | آخر صف `S1` | سقف `LOGS_FETCH_MAX`، ومن v2.0.0 وطالع |

🔴 **الحارس بيولّع لو أي منهم قال «اتطبع».** الميتافيلد ممكن يبقى فاضي والطباعة
حصلت فعلاً — `/track` بيرجّع `warning` لما التاج ينجح والميتافيلد يفشل. الاتجاه
الآمن هنا «اسأل زيادة» مش «اسكت».

### البوابة الموحّدة — `§PREPRINT-GATE`

نافذة **واحدة** بدل تلات نوافذ متتالية، وكل أوردر بيتعرض **مرة واحدة** تحت أعلى
فئة تنطبق عليه:

```
🔴 فاتورة لاغية / متغلّف   ← تأكيد إلزامي بمربع لكل أوردر
🟠 اتطبع خلال آخر ٣٠ دقيقة (REPRINT_WARN_MINUTES) ← تنبيه
🟡 سبق طباعته قبل كده                              ← تنبيه
✅ الباقي                                          ← سطر واحد بالعدد
```

- ⛔ **ممنوع ترجّعها تلات نوافذ.** أوردر واقع في التلاتة كان هيتعرض تلات مرات
  بتلات صياغات، والموظف يدوس Enter من غير ما يقرا (نفس منطق «سؤال ×٢١»).
- **مربع لكل أوردر** — الفعل فيزيائي ومتكرر (قوم دوّر على الورقة واقطعها).
  **مفيش «قطعت الكل»** بقرار: الحالات دي نادرة والعدد مايوصلش لرقم يستاهل مخرج جماعي.
- 🔴 **«اطبع الباقيين» و«إلغاء» عمرهم ما بيتعطّلوا.** «اطبع الكل» بس هو اللي
  متعطّل لحد ما كل المربعات تتظبط.
- 🔴 **فشل جلب السجل مايمنعش الطباعة** — توست وكمّل.
- الإقرار بيتسجّل في `extra.guard` بتاع صف الطباعة نفسه:
  `{ name, prevPrintAt, prevPackAt, statusAtGate, cutConfirmed }` —
  **من غير `type` جديد** في D1، فعقد `ecommoda-constants` §7 ما اتغيّرش.

### `MIN_WORKER_VERSION` اترفعت — والمرة دي بسبب

`2.1.0` → **`2.3.0`**. الواجهة بقت **معتمدة فعلاً** على `printingTimeS1` و
`packingTimeS1` في رد `/orders` (Standards #29 مستوفى — عكس حالة v3.4.0).

> 🔴 **Promote إلزامي للـ Worker v2.3.0.** من غيره الحقل مابيرجعش، والحارس
> بيشتغل بسجل D1 **بس**. التدهور آمن ومُعلَن: توست صريح «فحص الفاتورة اللاغية
> شغّال بالسجل بس — الـ Worker نسخة قديمة» + تحذير الهيدر. ممنوع الأداة تبان
> طبيعية وهي شايفة نص الصورة.

### ختم الفاتورة

كل فاتورة (S1 و S2) في آخرها سطر **7px** بلون `#c9c9c9` فيه تاريخ ووقت الطباعة
**بتوقيت القاهرة بالثانية** + اسم الموظف (`invoicePrintStamp`). الغرض الوحيد:
ورقتين لنفس الأوردر يبقى بينهم فرق **يتقرا**. ألوانه حرفية زي باقي الفاتورة
(مستند الـ iframe مالوش توكنز — استثناء #35).


## إعادة طباعة أوردر خرج من الطابور — Worker v2.4.0 (قرار أحمد 06-09-2026)

**المشكلة:** `/orders` بيفلتر على أربع حالات «مؤكَّد» بحكم تعريفه، وأوردر **اتطبع
خلاص** حالته `Ready` — يعني **مستحيل يوصل للجدول**. زرار «إعادة 🖨» في طابور
التغليف كان بيفتح صفحة الطباعة مفلترة على أوردر **مش موجود فيها**: الموظف يشوف
«لا توجد أوردرات تطابق الفلاتر» وتوست بيقول إن الجدول اتفلتر عليه.

**الورقة بتضيع أو تتقطع أو تطلع باهتة، والحل الوحيد كان إن حد يرجّع حالة الأوردر
يدوي** — وده تلات مشاكل مش واحدة:

| | الثمن |
|---|---|
| صف `metafields_change` بيقول `Ready → Confirmed + Edit` | **كذب في التاريخ** — بيغذّي حارس الفاتورة اللاغية بمعلومة غلط بعد كده |
| الأوردر بيختفي من طابور التغليف طول ما الحالة مرجّعة | لو الموظف نسي يطبع، **الأوردر بيتوه من الطابورين** بلا أي رسالة |
| محتاج أداة تالتة (تغيير الحالة) | خطوتين زيادة في نقطة شغل |

### اللي اتعمل

| # | التغيير | فين |
|---|---|---|
| ① | `POST /lookup` — أوردر واحد بالـ id أو بالاسم، **مهما كانت حالته**، بنفس شكل صف `/orders` بالحرف | `index.js` §LOOKUP |
| ② | `/invoice` بقى بياخد `type` **اختياري وصريح**، ومتحقَّق منه | `index.js` §PRINT::resolveInvoiceType |
| ③ | كارت «🔁 إعادة طباعة الفاتورة» في `print.html` بتاع الهب — **مش في الأداة المستقلة** | `Warehouse-Operations-Center/print.html` §REPRINT |

### 🔴 الحالة مابتتغيّرش — والفرع ده كان موجود من الأصل

```js
// setStatusToReady — الحالة الحالية = الهدف → مفيش تحوّل (إعادة طباعة)
if (statusBefore === target) return { written: false, skipped: 'same_value' };
```

إعادة طباعة أوردر `Ready` بتعمل: **تاج** + **`printing_time_sN` بالوقت الجديد** +
**صف `S1`/`S2` جديد في D1 باسم الموظف**، والحالة ما بتتلمسش والرد `success`.
يعني الباك إند كان بيدعم الحالة دي **قبل v2.4.0** — الناقص كان طريقة توصيل
الأوردر للواجهة بس.

> 🔴 **ميتافيلد وقت الطباعة بيتدهس عن قصد** (قرار أحمد 06-09-2026). الحقل معناه
> **آخر** طباعة مش أول واحدة (`known-gaps` G-4). الثمن اللي لازم يتقال: عمود
> «الوقت منذ الطباعة» في طابور التغليف بيرجع 🟢 أخضر، فأوردر متأخر ٣ أيام بيبان
> كأنه اتطبع دلوقتي. الكارت بيقول ده للموظف **قبل** ما يضغط.

### 🔴 لغم النوع — اللي البند ② اتكتب عشانه

```js
// قبل v2.4.0 — القاعدة الوحيدة في handleInvoice
const type = [CONFIRMED_RETURN, CONFIRMED_EXCHANGE].includes(status_2_r_e) ? 'S2' : 'S1';
```

أوردر **S2 حالته `Ready`** (وده بالظبط اللي في طابور التغليف) بيتقري **`S1`**.
والنوع ده **مش تسمية** — هو اللي بيختار بنود الفاتورة نفسها:

```js
if (type === 'S2') { if (li.fulfillableQuantity > 0 && !returnedIds.has(li.id)) … }
else               { if (li.currentQuantity > 0) … }
```

يعني **فاتورة ببنود غلط**. وأسوأ: الواجهة بتاخد النوع من رد `/invoice` وتبعته
لـ `/track`، و`/track` بيكتب على ماكينة **S1** — و`Shipped → Ready` تحوّل
**مسموح** في `ALLOWED_TRANSITIONS_S1`، يعني **كتابة صامتة على أوردر خرج من
المخزن**.

> ⚠️ **اللغم ده مكانش قابل للوصول قبل v2.4.0** — `/invoice` عمره ما اتنادى على
> أوردر `Ready`. `/lookup` هو اللي بيفتح المسار، فالنوع بقى بيتبعت صريح.

> 🔴 **الافتراضي ما اتغيّرش.** من غير `type` السلوك حرفيًا زي v2.3.0 — مسار
> الطابور **صفر تغيير**. القايمة الافتراضية (`S2_CONFIRMED_VALUES`) متسابة زي ما
> هي عن قصد: توسيعها كان هيغيّر تصنيف أوردر في طابور الطباعة **في صمت**.

### التلات قوايم بتاعة S2 — مش مترادفات

| القايمة | السؤال | مين بيستخدمها |
|---|---|---|
| `S2_CONFIRMED_VALUES` | «في طابور الطباعة كـ S2؟» | الافتراضي في `/invoice` + `inPrintQueue` |
| `S2_PRINTABLE_VALUES` | «الدورة S2 لسه في المخزن؟» (+`Ready`) | `/lookup` لتحديد النوع |
| `S2_ALL_VALUES` | «دخل ماكينة S2 أصلاً؟» | التحقق من `type` الصريح |

> ⚠️ `Shipped`/`In-Return`/`Returned` **مستبعدين من `S2_PRINTABLE_VALUES`** —
> الدورة خرجت من المخزن، فإعادة الطباعة بترجع لـ S1.

### `MIN_WORKER_VERSION` **ما اترفعتش** — والمرة دي ده الصح

`2.3.0` زي ما هي. **الأداة المستقلة** مش بتنادي `/lookup` ومش بتبعت `type`، فترفيع
الحد الأدنى هنا كان هيولّع تحذير كاذب على أي rollback مشروع (Standards #29 — نفس
حالة v3.4.0 بالظبط). **الهب** هو اللي رفع `printer.min` لـ `2.4.0` لأنه هو
المعتمد فعلاً.

> ⚠️ **`index.html` بتاع الأداة المستقلة اتغيّر فيه سطران بس** (`o.reprint` في
> البوابة و`o.forceType` في `executePrint`) — **مفيش حاجة هنا بتحطهم**، والسطرين
> ميّتين عن قصد: الكود ده مكرّر حرفيًا مع `print.html` بتاع الهب، والفرق الوحيد
> المسموح هو سطر `gateLink`. سطر متطابق ميّت أأمن من سطرين يفترقوا (درس R1).

## 🚚 قناة بوسطة — Worker v2.5.0 (قرار أحمد 07-09-2026)

**المشكلة اللي اتقفلت:** `ZONE_FILTER` كان بيستبعد `Other_Regions` من
`/orders` **من غير أي أثر على الشاشة**. و`Other_Regions` = **بوسطة**
(`ecommoda-order-lifecycle` → `references/zone-routing.md`)، وهي **أكبر قناة
في المتجر**: ٩٬٠٨٣ أوردر مقابل ٧٬١١٣ لـ `Cairo+Giza` (مقاس حي 06-09-2026).
يعني القناة الأكبر مالهاش طابور طباعة أصلاً.

### الزون قرار توجيه — مش جغرافيا

مقاس حي على ٥٠ أوردر: **٤ من ٢٢** عنوان قاهرة/جيزة (≈**١٨٪**) راحوا
`Other_Regions` — العبور المثال القايم (مافيش مندوب يغطيه). يعني الموظف
**مايقدرش** يستنتج القناة من العنوان، فالأداة بتقولها له صراحةً في عمود
ومبدّل. ⛔ وممنوع أي اشتقاق للزون من العنوان (قاعدة ١٦ في اللايف سايكل).

### العقد — تلات مراحل، وكل مرحلة ليها سبب

```
① ?action=bosta_lookup   أرقام أوردرات → شحنات      قراءة بحتة
② البوابة                من بيانات ① بلا أي نداء     السؤالين
③ ?action=bosta_awb      البوليصة PDF               قراءة بحتة
④ POST /track            تاج · ميتافيلد · Ready · D1  الكتابة الوحيدة
⑤ الطباعة
```

**⛔ مفيش `/invoice` في المسار ده خالص** — أوردر بوسطة بياخد **بوليصة بس،
من غير فاتورة** (قرار أحمد). يعني `INVOICE_QUERY` (الاستعلام التقيل اللي
سبّب R15) خارج مسار بوسطة تمامًا.

**⚠️ ④ قبل ⑤** — نفس ترتيب مسار الفاتورة بالحرف: التسجيل قبل فتح نافذة الطباعة.

### 🔴 «الماكينة» ≠ «المستند» — أخطر بند في الدمج

`/track` بياخد **حقلين منفصلين**، والخلط بينهم بيكسر الأداة **في صمت**:

| الحقل | القيمة لبوسطة | بيحكم إيه |
|---|---|---|
| `type` | **`S1`** | التاج `Printed(S1)` · `custom.printing_time_s1` · الحالة · جدول التحوّلات |
| `doc` | **`AWB`** | قيمة `type` في D1 **بس** |

لو بعتنا `type: 'AWB'`، `tagOrder` هتكتب تاج `Printed(AWB)` و
`setPrintingTimeMetafield` هتكتب مفتاح `printing_time_awb`. **الاتنين
مابيفشلوش** — التاج بيتكتب عادي والميتافيلد بيتعمل — و`isPrinted` يفضل
`false` للأبد، وأداة التغليف ماتشوفش وقت طباعة. **صفر خطأ في الكونسول.**

يعني على شوبيفاي أوردر بوسطة **مالوش أي تمييز**: طباعة S1 عادية بالظبط.
التمييز في D1 بس.

### الحُرّاس

| الحارس | بيمسك إيه |
|---|---|
| `AWB_BLOCKED_STATES` | شحنة ملغية · راجعة للتاجر · منتهية · ضايعة. بوسطة بترفضها، بس في **الوضع المجمّع بترفضها في صمت** — القايمة بتحوّل السقوط الصامت لصف **باسمه**، والبيانات جاية مجانًا من نداء البحث |
| `countPdfPages` | عدد صفحات الـ PDF == عدد الـ IDs. الحارس الوحيد ضد فخ `mass-awb` |
| `classifyForPrint` | أكتر من شحنة صادرة حيّة = **مافيش تخمين** — بيطلع من الدفعة |
| `not_found` | أوردر ما اترفعش على بوسطة — بوابة إلزامية اتخطّت |
| فرق التحصيل | `cod` عند بوسطة ≠ `currentTotalPriceSet` |

> ⚠️ **`AWB_BLOCKED_STATES` مقصود إنها ضيّقة.** لو قايمتنا أوسع من بوسطة
> نبقى منعنا طباعة مشروعة؛ لو أضيق، حارس عدّ الصفحات بيمسك الباقي.
> **الأوسع خطر، الأضيق مغطّى.**

### 🔴 `latestAWBPrintDate` للعرض بس — ممنوع يدخل أي حارس

بوسطة بتحدّث الحقل ده مع **كل** جلب للبوليصة — حتى لو الورقة ما خرجتش من
الطابعة أصلاً. يعني الأداة **بتلوّثه بنفسها**، وهو بيقول «حد جاب البوليصة»
مش «ورقة اتطبعت».

**النتيجة المنفَّذة:** حارس إعادة الطباعة لقناة بوسطة بيقرا من **سجل D1
لوحده** — نفس `REPRINT_WARN_MINUTES` ونفس `fetchTodayPrintMap` بتوع
قاهرة+جيزة بالظبط. والحقل بيتعرض في لوحة المعاينة كمعلومة وخلاص.

### 🔴 بوابة المراجعة — سؤالان لأوردر بوسطة

```
🔴 #53412  [بوسطة]  ☐ قطعت الورقة القديمة   ☐ عدّلت الشحنة على بوسطة
```

**السبب اللي رجّع الأوردر للطابور مش مهم للمخزن** (قرار أحمد) — المهم
الفعلين دول. والسؤال التاني موجود لأن **إعادة طباعة البوليصة مابتصلّحش
بيانات غلط**: البوليصة بتتولد من بيانات **بوسطة** مش من شوبيفاي، فالتعديل
مكانه داشبورد بوسطة. نفس نمط التأكيدين الإلزاميين في `remove.html`.

> ⚠️ `gateSyncState` بتفحص **كل مربع** مش كل صف — أوردر بوسطة عنده مربعين،
> والفحص على مستوى الصف كان هيعدّي بمربع واحد متعلّم.

> 🔴 **`bostaUpdated` لازم يفضل في `GUARD_KEYS`.** القايمة بيضا مقفولة:
> مفتاح مش فيها **بيتسقط في صمت**، فالبوابة تشتغل على الشاشة والسجل يقول
> إن الموظف ما أقرّش بحاجة.

### فخاخ بوسطة

- **إصداران مختلفان من API عن قصد:** البحث والمجمّع على `v2`، والبوليصة
  المفردة على `v1` (`v2` بيرجّع 404 عليها).
- **`ids` في `mass-awb` نص مفصول بفواصل** — مصفوفة بترجّع 500
  `ids.split is not a function`.
- **مكان الـ base64 مختلف:** `data.data` في المفرد · **`data`** مباشرةً في المجمّع.
- **`businessReferences` بالجمع بيتجاهله بوسطة في صمت** ويرجّع كل الشحنات
  لحد الـ `limit` — المفرد بـ CSV هو الشكل الصح.
- **البحث على شرائح ١٠** بـ `limit` ٥٠: أوردر الاستبدال عنده شحنتين،
  فـ ٢٥ مرجع ممكن يبقوا ٥٠ صف ويتقصّوا. والمطابقة النهائية **بالتساوي التام
  عندنا** — مطابقة جزئية معناها بوليصة أوردر تاني.
- **`type: 'SEND'` في جسم البحث بيتجاهله بوسطة** — الفلترة client-side.
- **قيم `type` من `/deliveries/search` مختلفة عن الويبهوك:** `Send` بحرف
  كبير واحد هنا، `SEND` هناك — المقارنة بلا حساسية لحالة الحروف.
- **`state.value` بترجّع "Delivered" للكودين 45 و46** — `STATE_MAP[state.code]`
  هو المصدر الوحيد.
- **`mass-awb` غير موثّق رسميًا** (مستنتَج من الداشبورد) — فبوسطة تقدر
  تغيّره من غير إشعار. عشان كده `diag` بيفحصه **بند دايم**، وفيه بند بيجاوب
  السؤال المفتوح: هل المفتاح الخام مقبول عليه أصلاً؟

## فخاخ الأداة دي

- 🔴 **`/lookup` قراءة بحتة — ممنوع يكتب أي حاجة.** أي ميوتيشن أو صف D1 هناك
  بيخلّي «شوف الأوردر» فعل، والواجهة بتنادي المسار ده **قبل** أي تأكيد من الموظف.
- 🔴 **البحث بالاسم في `/lookup` مطابقة تامة إلزامية.** فلتر `name:` في شوبيفاي
  بيطابق بالبادئة — `#5362` بيرجّع `#53621` كمان (نفس الفخ في `journey.html`).
  هنا الثمن **فاتورة أوردر تاني بتتحط في طرد**، فالمسار بيرجّع 404 بدل ما يخمّن.
- ⛔ **متوسّعش `S2_CONFIRMED_VALUES`** عشان تصلّح النوع — ده بيغيّر تصنيف أوردر في
  طابور الطباعة في صمت. `S2_PRINTABLE_VALUES` هي القايمة الموسّعة، ومكانها
  `/lookup` بس.
- 🔴 **`VOID_INVOICE_STATUSES` و`REPRINT_WARN_MINUTES` ثوابت مسمّاة** — تغيير أي
  واحدة فيهم **قرار** مش صدفة. وشيل مربع تأكيد من البوابة = شيل الحارس.
- **`printingTimeS1`/`packingTimeS1` بيرجعوا `null` لأوردرات S2** من الـ Worker
  عن قصد — قيمة S1 قديمة على أوردر معروض كـ S2 كانت هتولّع «فاتورة لاغية» على
  أوردر استبدال ما اتطبعش أصلاً.
- **`WORKER_SECRET` بتاع الأداة دي مش بتاعها لوحدها** — مجموعة `warehouse_ops`
  (فوق). تدويره لازم يحصل في التلات Workers مع بعض + Promote للتلاتة، وإلا
  الأداة اللي اتغيّر سرها بترفض السر القديم المخزّن في متصفح المخزن وترجّع
  `401` برسالة مضلّلة («تعذّر تحميل قائمة الموظفين») — `isConfigured()` بيفحص
  الطول بس، فالسر البايت شكله «مضبوط». البصمة في `diag` هي اللي بتفرّق.

- **`returns` كانت `first: 3` بدون `pageInfo`** — دورة رابعة كانت بتتقص
  **بصمت**. من v2.1.0 بقت `first: 10` و`returnLineItems`/`exchangeLineItems`
  `first: 50` + `pageInfo` على التلاتة، والقصّ بيرجع في `truncatedReturns`.
  `#30134` عنده ٤ دورات فعلاً — الحالة دي واقعة مش نظرية.
- **⛔ ممنوع ترجّع `Promise.all` على دفعة الطباعة.** `executePrint` بتستخدم
  `mapLimit(..., PRINT_CONCURRENCY, ...)` — الحد **مش** ضبط أداء، هو اللي
  بيمنع فشل الدفعة كلها (R15 فوق). و`mapLimit` بترجّع `{ ok }` / `{ error }`
  لكل عنصر بترتيب المدخلات — أي رجوع لـ `Promise.all` بيرجّع الباجين مع بعض:
  التزامن المفتوح **و** «واحد يفشل يلغي الكل».
- **`getAccessToken` متكاش من v2.2.0** — لو ضفت endpoint جديد بينادي شوبيفاي،
  مش محتاج تعمل حاجة (بينادي نفس الدالة). بس **متلغيش الكاش** عشان «الأداة
  بتتصرّف غريب» — الفرع الصح هو `invalidateAccessToken()` على 401، وهو موجود
  في `shopifyGQL` بالفعل.
- **`ZONE_FILTER` بيفلتر** — «الأوردر مش ظاهر» = افحص `custom.zone` الأول.
  و**الأوردر اللي مالوش `zone` خالص بيتستبعد هو كمان** (`undefined` مش في
  القايمة) — دي غالبًا السبب الحقيقي، مش «محافظة تانية».
  `/orders` لسه بيرجّع `zoneExcluded` في الرد، لكن **مش معروض في الواجهة**
  (المربّع اتشال بقرار أحمد 03-09-2026) — شوفه من رد الـ endpoint أو `diag`.
- **`Delivered` حالة نهائية** — أي محاولة طباعة S1 لأوردر متسلّم بترجّع
  `warning` والحالة **مش** بتتكتب. ده مقصود (`ecommoda-order-lifecycle` §1.4).
- **`/track` بيرجّع تلات حالات** — `success` · `warning` · `error`. الأصفر معناه
  الطباعة اتسجّلت بس فيه فعل ما تمّش (الحالة مثلاً). **ممنوع يتحسب نجاح.**
- **نتيجة الطباعة نافذة منبثقة من v3.2.0** — العدّادات التلاتة بتتعرض دايمًا،
  لكن **تفاصيل الأوردرات بتتبني بس لو فيه `warning` أو `error`**. النجاح الكامل
  = عدد بس. لو الرقم الأصفر/الأحمر أكبر من صفر ومفيش تفاصيل تحته، دي مشكلة في
  `renderTrackResults` مش سلوك مقصود.
- **الحالة الحالية بتتقرا قبل أي كتابة** — لو القراءة فشلت، الحالة **مش**
  بتتكتب والرد بيرجع `warning`. الاتجاه الآمن مقصود.
- **`order_id` مختلط في D1** — رقمي للجديد، GID للقديم (فوق).
- **تسميات النوع في الواجهة عرض بس** — «عادي» / «استبدال/استرجاع» بتيجي من
  `TYPE_LABEL` في الـ HTML. القيم المخزّنة في D1 وفي عقد الـ Worker بتفضل
  `S1`/`S2` حرفيًا — أي فلتر أو استعلام يستخدم القيم دي مش التسميات.
- **مستند الـ iframe مالوش وصول لـ `:root`** — كل ألوان HTML الفاتورة حرفية
  (`#000` / `#fff` / `#555`)، وده استثناء تقني موثّق من Standards #35 مش إهمال.
- **كل التواريخ في الواجهة بيوم القاهرة** (`cairoDayStr`) مش يوم UTC — فلاتر
  الفترة ومربّع «النهارده» والأعمدة. الفرق بيحط أي طباعة بين ١٢ و٣ الفجر في
  اليوم اللي فات، وده كان باج فعلي اتصلّح في v3.0.0.
- **فلترة الجدولين client-side** على البيانات المحمّلة — مقصود: الأداة بتحمّل
  السجل كله عشان تبني عدّاد «مرات الطباعة»، فمفيش pagination من السيرفر.
  `get_logs`/`get_logs_count`/`get_logs_export` موجودين في الـ Worker لكن
  الواجهة مش بتستخدمهم؛ التقصّ بيبان في بانر فوق جدول السجل.

## استرجاع النسخ القديمة

```
Indexv-iframe.html · Indexv-jspdf.html  →  commit 0f99338
Index-pdf.html (الأقدم)                 →  commit 85de4d6
النسخة قبل PR الأمان + المعمارية        →  commit e2cd6da

git show 0f99338:Indexv-iframe.html
```

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v2.0.0 |
| ecommoda-html-builder | v6.3.0 |
| ecommoda-order-lifecycle | v1.3.0 |
| ecommoda-constants | v1.6.0 (مجموعات السر §6) |

آخر مطابقة: 07-09-2026 · `index.js` v2.5.0 · `index.html` v3.5.1 (**مجمّدة على قاهرة+جيزة**)

## مسائل مفتوحة

- ⏳ **بند للمهارة مش للأداة:** قاعدة «السبب مع كتابة `manual_status`»
  (`ecommoda-order-lifecycle` §1.5) محتاجة **استثناء مكتوب للأدوات
  الأوتوماتيكية**. القاعدة اتكتبت للتغيير اليدوي (Order Status Updater)، وأداة
  الطباعة بتكتب `Ready` كأثر جانبي فالسبب ثابت ومعروف سلفًا.
  ✅ **محسوم لأداة الطباعة (أحمد، 03-09-2026):** بتسجّل السبب تلقائيًا
  (`notes: "S1: X → Ready (طباعة الفاتورة)"` + `extra.source`) **بدون** ما
  تسأل الموظف. الباقي: البند ده يتضاف للمهارة نفسها.
- **Log Tab v2 بفلترة server-side + pagination** — مؤجّل بقرار: الأداة محتاجة
  السجل كامل عشان عدّاد «مرات الطباعة»، فالفلترة client-side هي الصح هنا
  (`data-table-standard` § 7 بيسمح بالحالة دي بشرط التوثيق — وده التوثيق).
  لو السجل كبر لدرجة إن السقف بقى بيتقص كتير، القرار يتراجع.
- **تضييق `Build watch paths`** على `index.js` + `wrangler.toml` (§13-ب).

### ✅ اتضاف في v3.5.1 / Worker v2.4.0 (قرار أحمد 06-09-2026)

- **إعادة طباعة أوردر خرج من الطابور** — `POST /lookup` + `type` صريح في
  `/invoice` (القسم فوق).
- 🔴 **مطلوب Promote** — من غيره كارت إعادة الطباعة في الهب بيقول بالنص
  «محتاجة Worker v2.4.0»، وباقي الصفحة شغّال عادي. التدهور آمن ومُعلَن،
  بس الزرار مايشتغلش.
  ⚠️ **Promote واحد لـ v2.5.0 بيغطّي البندين** (إعادة الطباعة وقناة بوسطة) —
  v2.4.0 ما اتعملهاش Promote منفصل.
- ⏳ **مدخل يدوي لإعادة طباعة أي أوردر بالرقم** لسه ما اتعملش — دلوقتي المسار
  بيتفتح من `?order=` بس (يعني من طابور التغليف). الـ Worker بيدعم البحث بالاسم
  خلاص، فده تمريرة واجهة صغيرة لو اتطلبت.

### 🔴 مطلوب بعد v2.5.0 — بالترتيب

1. **تسجيل `AWB` و`not_found` تحت `order_printer` في `ecommoda-constants` §7**،
   و`bosta_awb_print` كقيمة **متقاعدة**. الأداة بتكتب القيم دي، والتسجيل
   شرط قبل أول `writeLog` (Rule 7 في `ecommoda-worker-builder`).
2. **`BOSTA_API_KEY` سر جديد على `order-printer-worker`** — نفس مفتاح حساب
   بوسطة الموجود على `orders-packing-checker-worker`.
3. **Promote للـ Worker v2.5.0.** من غيره `?action=bosta_lookup` بترجّع 404
   وقناة بوسطة في الهب مش شغّالة أصلاً، وحارس النسخة بيقول «Worker الطباعة
   نسخة قديمة» (`printer.min = 2.5.0` في الهب).
4. **🩺 فحص النظام بعد الـ Promote** — البنود الأربعة الجديدة لازم يعدّوا،
   وأهمهم **«بوسطة — الطباعة المجمّعة (mass-awb)»**: ده بيجاوب السؤال
   المفتوح «هل المفتاح الخام مقبول على المجمّع؟». 401 هناك معناه إن الدفعة
   هترجع لنداء مفرد لكل شحنة.
5. **قياس `zoneExcluded`** من `/orders` القديم قبل النشر — بيقول كام أوردر
   في الطابور زونه فاضي أو غير معروف. لو الرقم كبير، الصفوف دي محتاجة قسم
   منفصل مش سطور وسط الجدول.

### ✅ اتضاف في v3.5.0 / Worker v2.3.0 (قرار أحمد 06-09-2026)

- **حارس الفاتورة اللاغية** + بوابة المراجعة الموحّدة + ختم الفاتورة (القسم فوق).
- 🔴 **مطلوب Promote للـ Worker v2.3.0** — من غيره الحارس شغّال بنص مصادره.
- ⏳ **S2 خارج النطاق بقرار** — نفس المنطق ينطبق على `printing_time_s2` لكن
  ما اتنفّذش. الكود عام، والإضافة تمريرة صغيرة لو اتطلبت.

### ✅ اتقفلت في v3.4.0 / Worker v2.2.0 (بلاغ المخزن 05-09-2026)

- ~~**R15** — دفعة طباعة كبيرة بتفشل كلها برسالة CORS~~ — حد تزامن + فشل جزئي
  + كاش توكن + `Max-Age` (القسم فوق).
- ~~بند تحريري: النسخة `v3.3.0` ماكانش ليها إدخال في الـ Changelog، وبادج «آخر
  نسخة» كان لسه على `v3.2.0`~~ — الإدخال اتضاف والبادج اتنقل.

### ✅ اتقفلت في v2.1.0 (مراجعة الكود 03-09-2026)

- ~~**R7** — تجميع كل دورات الإرجاع في الفاتورة~~ — بقت أحدث دورة بس (القسم فوق).
- ~~**R8** — `returns(first: 3)` بدون علم اقتطاع~~ — تصفيح + `truncatedReturns`.
- ~~**R6** — مفيش حارس لـ `WORKER_SECRET` الغايب~~ — اتضاف، و`diag` بيفحصه.
- ~~**R12** — `compatibility_date` غير موحّد~~ — اتحرّك لـ 2026-09-03 بعد فحص
  الأعلام على الكود الفعلي (القسم فوق). **مش** «فاتساب» زي ما كان مكتوب هنا.

آخر تحديث: 07-09-2026

</div>
